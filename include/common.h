#ifndef COMMON_H_
#define COMMON_H_

#include <inttypes.h>

/**
 * All elements in the input arrays are smaller than MAX_ELEM.
 *
 * So it should really be called "MAX_ELEM_PLUS_ONE".
 */
constexpr uint32_t MAX_ELEM = 20000000;

#endif // #ifndef COMMON_H_
