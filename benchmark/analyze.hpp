#ifndef ANALYZE_H_
#define ANALYZE_H_

#include <vector>
#include <inttypes.h>

void analyze(const std::vector<std::vector<uint32_t>>& data,
             const std::vector<std::vector<uint32_t>>& queries);

#endif
